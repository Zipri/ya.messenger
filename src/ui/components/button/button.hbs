<button
  id="{{id}}"
  class="{{styleClasses}}"
  {{#if type}}type="{{type}}"{{/if}}
  {{#if disabled}}disabled{{/if}}
>
  {{#if text}}
    {{text}}
  {{/if}}
  {{#if content}}
    {{{content}}}
  {{/if}}
</button>
